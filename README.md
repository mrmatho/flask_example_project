# Sample Flask Workout Tracker -  for VLP Reference

This is a simple Flask application that allows users to track their workouts. Users can add new workouts, view their workout history, and see a summary of their progress. The application uses a CSV file to store workout data.

## Usage

1. Clone the repository (if using git) or download and unzip to the project directory.
2. Install the required dependencies:
   ```
   pip install flask
   ```
3. Run the application:
   ```
    python app.py
    ```

## Reminder

- Project is for reference and may have missing features or elements.
- Not intended for distribution to students, just for reference for VLP writers.
- Code can be copied, used in slides, and adapted in any way needed for the VLP project.
